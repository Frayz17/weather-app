import { makeStyles } from '@material-ui/core/styles';

const CityListStyle = makeStyles((theme) => ({
  root: {
    display: 'flex',
    alignItems: 'center',
    padding: 5,
    marginTop: 50
  }
}));

export default CityListStyle;
