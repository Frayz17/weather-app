import { makeStyles } from '@material-ui/core/styles';

const weatherTempStyle = makeStyles((theme) => ({
  root: {
    display: 'flex',
    alignItems: 'center'
  }
}));

export default weatherTempStyle;
