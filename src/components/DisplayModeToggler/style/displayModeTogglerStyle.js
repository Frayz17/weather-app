import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles((theme) => ({
  btn: {
    position: 'absolute',
    top: 0,
    left: 0,
    // color: '#EC6E4C',
  },
}));

export default styles;
