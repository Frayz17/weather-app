import { makeStyles } from '@material-ui/core/styles';

const CityForecastStyle = makeStyles((theme) => ({
  root: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    padding: 5
    // marginTop: 50,
  }
}));

export default CityForecastStyle;
